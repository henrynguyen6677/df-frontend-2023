import {ID_NAMES} from "../../contants/ids.constant";
import {CLASS_NAMES} from "../../contants/classes.constant";

export default function MainComponent () {
  return <div id={ID_NAMES.main} className={CLASS_NAMES.backgroundGray}>

  </div>
}
